package model;

public enum TokenType {
        MINUS,
        PLUS,
        MULTIPLY,
        DIVIDE,
        GEQ,
        GREATER,
        LEQ,
        LESS,
        IDENTIFIER,
        STRING,
        NUMBER,
        BOOLEAN,
        PRINT,
        LET,
        CONST,
        IF,
        ELSE,
        NUMBER_TYPE,
        STRING_TYPE,
        BOOLEAN_TYPE,
        SEMICOLON,
        COLON,
        ASSIGNATION,
        LEFT_PAREN,
        RIGHT_PAREN,
        LEFT_CURLY_BRACES,
        RIGHT_CURLY_BRACES,
        EOF
    }
